<% if defined?(f) %>
  <%= render 'sections/section_inner', :f => f, :section => section %>
<% else %>
  <%= fields_for "chapters[]", section.chapter do |f| %>
    <%= render 'sections/section_inner', :f => f, :section => section %>
  <% end %>
<% end %>
