<%= render 'layouts/errors', object: @section %>
<%= section_form do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :week, 'Section week' %>
    <%= f.text_field :week, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :github_path, 'Section Github URL' %>
    <%= f.text_field :github_path, class: 'form-control' %>
  </div>
  <p class="alert alert-info">Note that building a section from Github will take awhile.</p>
  <div class="form-group">
    <div class="checkbox">
      <%= f.label :public, class: "checkbox" do %>
        <%= f.check_box :public %>
        Public
      <% end %>
    </div>
  </div>
  <%= f.submit :class => "btn btn-info" %>
<% end %>
