<% provide :title, "Edit #{@section.name}" %>

<h1><%= yield :title %></h1>

<%= render 'form' %>

<p><%= link_to "Delete #{@section.name}", @section, :confirm => 'Are you sure?', :method => :delete %></p>

<%= render 'navbar' %>
